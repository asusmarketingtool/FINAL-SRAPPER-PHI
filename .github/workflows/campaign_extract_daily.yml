name: Daily Extracts (Campaign + Ads Dialog) Chile 10:00

on:
  schedule:
    # 09:00 en Chile (UTC-3) → 12:00 UTC
    - cron: "0 12 * * *"
  workflow_dispatch: {}    # manual: siempre corre

jobs:
  daily-extracts:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install playwright gspread google-auth
          python -m playwright install --with-deps chromium

      # 1️⃣ Campaign Extract
      - name: Run Campaign Extract
        env:
          GCP_SA_JSON: ${{ secrets.GCP_SA_JSON }}
        run: |
          echo "🚀 Iniciando Campaign Extract..."
          python campaign_extract_to_sheets.py
          echo "✅ Campaign Extract finalizado."

      # 2️⃣ Ads Dialog Extract (actualiza EXTRACT_LIM)
      - name: Run Ads Dialog Extract
        env:
          GCP_SA_JSON: ${{ secrets.GCP_SA_JSON }}
        run: |
          echo "🚀 Iniciando Ads Dialog Extract..."
          python test_ads_dialog_extract_v5.py
          echo "✅ Ads Dialog Extract finalizado."

      # 3️⃣ Notificación por correo desde asusmarketingtool@gmail.com
      - name: Send Email Notification
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          # Gmail SMTP
          server_address: "smtp.gmail.com"
          server_port: "587"

          # Credenciales de la cuenta GMAIL asusmarketingtool@gmail.com
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}

          # Remitente y destinatario
          from: "asusmarketingtool@gmail.com"
          to: "josepedro_montes@asus.com"

          subject: >
            [Daily Extracts] Estado del job: ${{ job.status }}

          body: |
            Hola Chepe,

            El workflow "Daily Extracts (Campaign + Ads Dialog) Chile 10:00" acaba de ejecutarse.

            Estado del job: ${{ job.status }}

            Detalles:
            - Repositorio: ${{ github.repository }}
            - Branch: ${{ github.ref_name }}
            - Run ID: ${{ github.run_id }}
            - Evento: ${{ github.event_name }}

            Link directo al run:
            https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

            (Correo automático enviado desde asusmarketingtool@gmail.com via GitHub Actions.)

